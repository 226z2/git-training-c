# Git 协作规则

## 必须遵守

- 从教学仓库 Fork 到自己的账号。
- Clone 自己的 Fork 仓库。
- 从新分支提交，不要直接在 `main` 分支上做作业。
- 分支名使用 `student/<学号>/task01`。
- 每个 PR 只提交本次任务相关内容。
- commit message 要能说明修改目的。

## 禁止提交

- 编译产物，例如 `.exe`、`.o`、`.obj`、`.dll`。
- 压缩包，例如 `.zip`、`.rar`、`.7z`。
- 私钥、token、密码、账号凭据。
- 与任务无关的大文件。
